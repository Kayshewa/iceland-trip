# Iceland South Coast — 5-Day Trip Companion

Self-contained single-page trip app, deployed via GitHub Pages to https://tripiandkayshewa.com

- `index.html` — the deployable site (all images/fonts embedded)
- `index_tmpl.html` + `data.json` + `*.css` + `geo.json`/`map.json` — build inputs
